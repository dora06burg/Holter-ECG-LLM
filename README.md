# Holter-ECG-LLM

This repository currently contains the local `ECG_Model/` folder synced from the working environment.

Notes:
- The dataset and generated outputs are ignored by default via `.gitignore`:
  - `ECG_Model/dataset/`
  - `ECG_Model/stream_result/`
  - `ECG_Model/viz_output/`
- Put your dataset/output files locally in those folders as needed.

